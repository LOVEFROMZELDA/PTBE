# Interaction interface

An application for interaction interface.

---
To use the app, MATLAB Runtime(R2022b) should be installed firstly, following the process in
`Interaction interface/App/for_redistribution_files_only/readme.txt`.

After everything is prepared, `App.exe` could be run directly.
The code of the app is in `App.mlapp`.


# Discrete gesture recognition

Code files for signal preprocessing and nerual network training of Discrete gesture recognition.

---

`data_origin.zip` `data_origin.z01` contains the original data obtained by experiments.
After preprocessed by `signal_segmentation.m`, `data.zip` is used as the dataset for the nerual network training.
The specific network is in `NET.mat`.

# 200×200 virtual array

Code files for dataset generation and nerual network training of 200×200 virtual array.

---

The dataset is generated based on a waveform sample, which is obtained by experiment.
`dataset.zip` contains the dataset generated by `dataset_generation.m`.
The specific network is in `NET.mat` and is trained using `signal_classification.m`. 
